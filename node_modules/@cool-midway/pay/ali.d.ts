import { CoolAliPayConfig } from './interface';
import AlipaySdk from 'alipay-sdk';
/**
 * 支付宝支付
 */
export declare class CoolAliPay {
    pay: AlipaySdk;
    coolAlipay: CoolAliPayConfig;
    init(): Promise<void>;
    /**
     * 获得支付宝支付SDK实例
     * @returns
     */
    getInstance(): AlipaySdk;
    /**
     * 通知验签
     * @param postData {JSON} 服务端的消息内容
     * @param raw {Boolean} 是否使用 raw 内容而非 decode 内容验签
     */
    signVerify(postData: any, raw?: boolean): boolean;
    /**
     * 创建订单
     * @param length 订单长度
     * @returns
     */
    createOrderNum(length?: number): any;
    /**
     * 动态配置支付参数
     * @param config 微信配置
     * @returns
     */
    initPay(config: CoolAliPayConfig): AlipaySdk;
}
