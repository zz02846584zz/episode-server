import { CoolTxConfig } from './interface';
/**
 * 腾讯云短信
 */
export declare class SmsTx {
    config: CoolTxConfig;
    /**
     * 配置
     * @param config
     */
    setConfig(config: CoolTxConfig): void;
    /**
     * 发送短信
     * @param phone 手机号
     * @param params 参数
     * @param config  signName 签名 template 模板
     * @returns
     */
    send(phone: string, params: string[], config?: {
        signName: string;
        template: string;
    }): Promise<import("tencentcloud-sdk-nodejs/tencentcloud/services/sms/v20210111/sms_models").SendSmsResponse>;
}
