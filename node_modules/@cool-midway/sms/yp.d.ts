import { CoolYpConfig } from "./interface";
/**
 * 云片短信
 */
export declare class SmsYp {
    config: CoolYpConfig;
    /**
     * 配置
     * @param config
     */
    setConfig(config: CoolYpConfig): void;
    /**
    * 发送短信
    * @param phones 手机号 数组，需要加国家码如 ["+8612345678901"]
    * @param params 参数
    * @param config  signName 签名 template 模板
    * @returns
    */
    send(phones: string, params: {
        [key: string]: string;
    }, config?: {
        signName: string;
        template: string;
    }): Promise<boolean>;
    /**
     * 获得短信模板值
     * @param obj
     * @returns
     */
    protected smsTplValue(obj: any): string;
}
