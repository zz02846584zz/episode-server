import { SmsYp } from "./yp";
import { SmsAli } from "./ali";
import { SmsTx } from "./tx";
import { CoolSmsConfig } from "./interface";
export declare class CoolSms {
    smsYp: SmsYp;
    smsAli: SmsAli;
    smsTx: SmsTx;
    config: CoolSmsConfig;
    /**
     * 配置
     * @param config
     */
    setConfig(config: CoolSmsConfig): void;
    /**
     * 发送验证码 模板字段名为：code
     * @param phone
     * @param config
     */
    sendCode(phone: any, config?: {
        signName: string;
        template: string;
    }): Promise<number>;
    /**
     * 发送短信
     * @param phone
     * @param params
     * @param config
     * @returns
     */
    send(phone: string, params: any, config?: {
        signName: string;
        template: string;
    }): Promise<unknown>;
    /**
     * 生成验证码
     */
    generateNumber(digits?: number): number;
}
