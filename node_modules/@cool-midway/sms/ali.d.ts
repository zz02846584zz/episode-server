import { CoolSmsAliConfig } from './interface';
/**
 * 阿里云短信
 */
export declare class SmsAli {
    config: CoolSmsAliConfig;
    /**
     * 配置
     * @param config
     */
    setConfig(config: CoolSmsAliConfig): void;
    /**
     * 发送短信
     * @param phone 手机号
     * @param params 参数
     * @param config  signName 签名 template 模板
     * @returns
     */
    send(phone: any, params: {
        [key: string]: string;
    }, config?: {
        signName: string;
        template: string;
    }): Promise<unknown>;
}
