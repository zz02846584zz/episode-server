import { ILogger, IMidwayApplication } from '@midwayjs/core';
import { BaseCoolQueue } from './base';
/**
 * 任务队列
 */
export declare class CoolQueueHandle {
    redisConfig: any;
    coreLogger: ILogger;
    app: IMidwayApplication;
    redis: any;
    init(): Promise<void>;
    /**
     * 扫描队列
     */
    scan(): Promise<void>;
    /**
     * 获得锁
     * @param key 键
     * @param expireTime 过期时间
     * @returns
     */
    getLock(key: any, expireTime: any): Promise<boolean>;
    /**
     * 队列名称
     * @param cls
     * @param mod
     */
    createQueue(cls: BaseCoolQueue, mod: any): Promise<void>;
}
