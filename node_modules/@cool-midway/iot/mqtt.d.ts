import { CoolIotConfig } from './interface';
import { ILogger, IMidwayApplication } from '@midwayjs/core';
import Aedes from 'aedes';
/**
 * MQTT服务
 */
export declare class CoolMqttServe {
    coolIotConfig: CoolIotConfig;
    coreLogger: ILogger;
    serve: Aedes;
    app: IMidwayApplication;
    init(): Promise<void>;
    /**
     * 开启服务
     */
    startServe(): Promise<void>;
    /**
     * 初始化服务
     */
    initServe(): Promise<void>;
    /**
     * 处理类
     */
    handlerCls(): Promise<void>;
    /**
     * 处理事件
     * @param module
     */
    handlerEvent(module: any): Promise<void>;
    /**
     * 发送消息
     * @param topic 话题
     * @param message 消息
     * @param other 其他配置
     */
    publish(topic: any, message: any, other?: any): Promise<void>;
}
