import { IOption } from './interface';
export default class LightSpinner {
    text: any;
    private index;
    private timeout;
    private spinners;
    private message;
    private timeHandler;
    private isWin;
    constructor(config?: IOption);
    start(): void;
    stop(): void;
    private clearLine;
    private output;
    private doing;
}
